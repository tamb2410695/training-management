ChatGPT said:
Dưới đây là context tổng hợp toàn bộ quá trình xây dựng Documents Module. Có thể copy nguyên khối này sang cuộc trò chuyện mới để tiếp tục.

CONTEXT PROJECT: Documents Module - Upload / Download / Access Control
1. Kiến trúc project
Project sử dụng module-based architecture:

src/modules/

├── accounts
├── auth
├── classes
├── courseCategories
├── courses
├── dashboard
├── documents
├── enrollments
├── registrations
├── roles
├── staffs
├── students
└── users

Convention module:

module/

├── module.constants.js
├── module.controller.js
├── module.formatter.js
├── module.middleware.js
├── module.repository.js
├── module.routes.js
├── module.service.js
├── module.validator.js
└── index.js

2. Layer architecture
Flow chuẩn:

Controller

    |
    v

Service

    |
    v

Repository

    |
    v

Database

Controller
Chỉ:

nhận request
lấy params/body/user
gọi service
trả response
Không chứa:

SQL
business logic
permission logic
Service
Chứa:

business logic
transaction
permission resource
orchestration giữa module
Repository
Chứa:

SQL
database access
mapping snake_case/camelCase
Không chứa:

permission
business rule
3. Documents module hiện tại
documents/

├── documents.constants.js
├── documents.controller.js
├── documents.formatter.js
├── documents.mapper.js
├── documents.middleware.js
├── documents.repository.js
├── documents.routes.js
├── documents.service.js
├── documents.validator.js
├── index.js
│
└── storage/

    ├── index.js
    ├── storage.constants.js
    ├── storage.helper.js
    ├── mime.helper.js
    ├── storage.service.js
    ├── file.validator.js
    │
    └── providers/

        ├── index.js
        ├── provider.factory.js
        └── local.provider.js

4. Database DOCUMENT
Schema:

CREATE TABLE DOCUMENT (
  document_id INT AUTO_INCREMENT PRIMARY KEY,

  course_id INT NOT NULL,

  uploaded_by INT NOT NULL,

  document_code VARCHAR(30),

  title VARCHAR(255) NOT NULL,

  description VARCHAR(255),

  category VARCHAR(50),

  file_path VARCHAR(255),

  original_name VARCHAR(255),

  stored_name VARCHAR(255),

  mime_type VARCHAR(100),

  extension VARCHAR(10),

  file_size BIGINT,

  is_visible BOOLEAN DEFAULT TRUE,

  document_status VARCHAR(25)
      DEFAULT 'AVAILABLE',

  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,

  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
      ON UPDATE CURRENT_TIMESTAMP,

  deleted_at DATETIME NULL
);

Relationship:

DOCUMENT

 |
 |
 +---- COURSE

 |
 |
 +---- STAFF_PROFILE
              |
              |
           ACCOUNT

5. Documents Repository
File:

documents.repository.js

Có:

find()

findById()

findByCode()

create()

update()

remove()

find()
Hỗ trợ:

Search
documentCode
title
description
category
originalName
courseName
uploaderName

Sort
documentId
documentCode
title
category
fileSize
documentStatus
isVisible
createdAt
updatedAt
courseName
uploaderName

Filter
courseId
uploadedBy
documentStatus
isVisible

Soft delete:

Mọi query:

deleted_at IS NULL

Remove:

UPDATE DOCUMENT

SET
 document_status='DELETED',
 deleted_at=CURRENT_TIMESTAMP

Không xóa file vật lý.

Quyết định:

Database soft delete
Physical file giữ lại
6. Storage Architecture
Mục tiêu:

Documents module không biết storage cụ thể:

documents.service

        |

        v

storage.service

        |

        v

provider.factory

        |

        +---- local

        +---- s3

        +---- minio

Storage service API
Hiện có:

normalize()

save()

remove()

exists()

getDownloadInfo()

Provider contract
Mỗi provider:

save()

exists()

remove()

getDownloadPath()

7. Local Provider hiện tại
File:

storage/providers/local.provider.js

Chức năng:

lưu file
kiểm tra tồn tại
lấy absolute path
không xóa vật lý trong Documents lifecycle
8. Upload flow hiện tại
Flow:

multipart/form-data

        |

        v

multer middleware

        |

        v

controller.upload

        |

        v

service.upload()

        |

        + validate file

        |

        + validate course

        |

        + normalize file

        |

        + storage.save()

        |

        + mapper.buildCreatePayload()

        |

        + repository.create()

        |

        + generate documentCode

        |

        + update documentCode

        |

        v

response

9. Auth hiện tại
Middleware:

authenticate

Flow:

Authorization Bearer Token

        |

        v

verify JWT

        |

        v

req.user

JWT payload:

{
 accountId,
 roleCode,
 staffId
}

authorize:

authorize(
 "ADMIN",
 "STAFF"
)

Flow:

authenticate

       |

       v

authorize(role)

       |

       v

controller

10. Quy tắc Permission Documents
Không tạo permission riêng trong Documents.

Role permission:

Route:

authenticate

        |

authorize(role)

Resource permission:

Service.

Ví dụ:

Document:

uploadedBy = 10

Staff:

staffId = 20

Không được update/delete.

11. documents.constants.js hiện tại
Đã phân tích.

Cần chỉnh:

BODY.CREATE
Không nên nhận:

uploadedBy

filePath

originalName

storedName

mimeType

extension

fileSize

Frontend chỉ gửi:

CREATE:[
 "courseId",
 "title",
 "description",
 "category",
 "isVisible"
]

REQUIRED.CREATE
Bỏ:

uploadedBy

Chỉ:

[
 "courseId",
 "title"
]

UPDATE
Không cho sửa:

documentStatus

documentCode

Nên:

UPDATE:[
 "title",
 "description",
 "category",
 "isVisible"
]

12. documents.mapper.js
Hiện tại:

buildCreatePayload()
Đúng.

Nguồn:

staffId

từ JWT.

Sinh:

uploadedBy

Storage sinh:

filePath
originalName
storedName
mimeType
extension
fileSize

buildUpdatePayload()
Dùng:

DOCUMENT_FIELDS.BODY.UPDATE

Sau khi constants sửa thì tự đồng bộ.

buildDownloadResponse()
Cần đồng bộ với storage contract.

Mục tiêu:

{
 path,
 fileName,
 mimeType
}

13. documents.formatter.js
Chức năng:

API response formatting.

Đã tốt:
formatList()

formatDetail()

formatFile()

formatCourse()

formatUploader()

Cần sửa:
formatDownload()
Hiện sai:

fileName: info.originalName

Storage contract mới:

{
 path,
 fileName,
 mimeType
}

Nên:

const formatDownload = (info)=>{

 return {
   fileName: info.fileName,

   mimeType: info.mimeType,

   path: info.path,
 };

};

Boolean conversion
Hiện:

Boolean("false")

sai.

Cần helper:

"false" -> false

"true" -> true

14. documents.service.js hiện tại
Upload
Hiện:

upload(
 documentData,
 fileInfo,
 staffId
)

Đúng.

Cần sửa response:

Hiện:

return documentsRepository.findById()

trả raw object.

Nên:

const document =
 await documentsRepository.findById();

return documentsFormatter.formatDetail(document);

Update
Hiện:

return updatedDocument;

Nên:

return documentsFormatter.formatDetail(
 updatedDocument
);

Download
Flow:

find document

 |

check status AVAILABLE

 |

storage.exists()

 |

storage.getDownloadInfo()

 |

return download response

Cần thống nhất:

storage:

getDownloadInfo()

trả:

{
 path,
 fileName,
 mimeType
}

15. documents.routes.js hiện tại
Đã tích hợp:

authenticate

authorize

nhưng đang comment.

Mapping dự kiến:

GET list
ADMIN
STAFF
STUDENT

GET detail
ADMIN
STAFF
STUDENT

POST upload
ADMIN
STAFF

PUT update
ADMIN
STAFF

DELETE remove
ADMIN
STAFF

DOWNLOAD
ADMIN
STAFF
STUDENT

Visibility check trong service.

16. documents.controller.js
Hiện tại:

Upload:

documentsService.upload(
 req.body,
 req.file,
 req.user.staffId
)

Đúng.

Update/remove hiện chưa truyền:

req.user.staffId

Cần bổ sung khi triển khai resource ownership.

17. Chưa hoàn thành
Phase 7 - Access Control
7.1 Route authorization
Cần bật:

authenticate

authorize()

7.2 Resource ownership
Cần thêm:

validateDocumentOwner()

Dùng cho:

update()

remove()

Logic:

document.uploadedBy
===
currentStaffId

7.3 Visibility
Download:

Student:

document.isVisible === true

18. Chưa làm: Storage lifecycle
Đã quyết định:

Không xóa file vật lý.

Delete:

Database:

document_status='DELETED'

deleted_at=current_timestamp

File:

Giữ nguyên.

19. Trạng thái hiện tại
Đã hoàn thành:

Storage abstraction       DONE

Provider pattern          DONE

Upload middleware         DONE

File validation           DONE

Mapper                    DONE

Repository                DONE

Soft delete               DONE

Đang hoàn thiện:

Upload response contract

Download contract

Formatter consistency

Permission integration

20. Việc tiếp theo trong cuộc trò chuyện mới
Ưu tiên thứ tự:

Bước 1
Hoàn thiện:

storage.service.js

local.provider.js

documents.service.download()

để download chạy ổn định.

Bước 2
Chỉnh:

documents.service.js

format upload response
format update response
Bước 3
Bật:

documents.routes.js

authenticate()

authorize()

Bước 4
Thêm:

validateDocumentOwner()

cho:

update()

remove()

Context kết thúc tại đây. Tiếp tục xây dựng từ:

"Hoàn thiện Upload + Download cơ bản của Documents Module trước, sau đó triển khai Access Control."